# ============================================
# Variáveis de Ambiente - Plataforma de Gestão para Grupos de Networking
# ============================================
# 
# Este arquivo contém todas as variáveis de ambiente necessárias para executar
# a aplicação. Copie este arquivo para .env.local e preencha com seus valores reais.
#
# IMPORTANTE: 
# - Nunca commite o arquivo .env.local no Git
# - Use valores seguros e únicos em produção
# - Para mais detalhes, consulte o README.md
#
# ============================================

# ============================================
# MongoDB Configuration
# ============================================

# URI completa de conexão com MongoDB (OBRIGATÓRIA)
# Formato para MongoDB Atlas:
#   mongodb+srv://usuario:senha@cluster.mongodb.net/ag-sistemas
# Formato para MongoDB local:
#   mongodb://localhost:27017/ag-sistemas
MONGODB_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/ag-sistemas

# Nome do banco de dados (OPCIONAL)
# Se não especificado, o sistema usa 'ag-sistemas' como padrão
# Pode estar incluído na URI do MONGODB_URI
MONGODB_DB_NAME=ag-sistemas

# ============================================
# Admin Authentication
# ============================================

# Token secreto para autenticação administrativa (OBRIGATÓRIA)
# Use um valor seguro e único. Gere usando:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#   openssl rand -hex 32
ADMIN_TOKEN=seu_token_secreto_super_seguro_aqui

# ============================================
# Application Configuration
# ============================================

# URL base da aplicação (OBRIGATÓRIA)
# Usado para gerar links de convite e redirecionamentos
# Desenvolvimento: http://localhost:3000
# Produção: https://seu-dominio.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# JWT Configuration
# ============================================

# Secret para assinatura de tokens JWT (OBRIGATÓRIA)
# Deve ter no mínimo 32 caracteres para segurança adequada
# Gere usando:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#   openssl rand -hex 32
JWT_SECRET=seu_jwt_secret_super_seguro_minimo_32_caracteres

# Tempo de expiração do access token (OPCIONAL)
# Padrão: 15m (15 minutos)
# Formatos aceitos: 15m, 1h, 30m, etc.
JWT_ACCESS_EXPIRES_IN=15m

# Tempo de expiração do refresh token (OPCIONAL)
# Padrão: 7d (7 dias)
# Formatos aceitos: 7d, 30d, 14d, etc.
JWT_REFRESH_EXPIRES_IN=7d

# ============================================
# Notas Adicionais
# ============================================
#
# Para gerar tokens seguros:
#   - Node.js: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#   - OpenSSL: openssl rand -hex 32
#
# Para mais informações sobre configuração:
#   - Consulte o README.md na raiz do projeto
#   - Consulte o ARQUITETURA.md para detalhes técnicos
#   - Consulte Docs/Documentation/DEPLOY.md para deploy em produção
#
# ============================================
