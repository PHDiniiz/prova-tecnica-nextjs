# MongoDB Configuration
# URI de conexão do MongoDB (obrigatória)
# Exemplo: mongodb+srv://usuario:senha@cluster.mongodb.net/
MONGODB_URI=mongodb+srv://...

# Nome do banco de dados (opcional, padrão: 'networking-platform')
MONGODB_DB_NAME=networking-platform

# Autenticação Admin
# Token secreto para autenticação de rotas administrativas (obrigatória)
# Gere um token seguro e único para produção
ADMIN_TOKEN=seu_token_secreto_aqui

# Aplicação
# URL pública da aplicação (opcional, padrão: 'http://localhost:3000')
# Usado para gerar links de convite e redirecionamentos
NEXT_PUBLIC_APP_URL=http://localhost:3000
