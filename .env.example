# MongoDB Configuration
# Para MongoDB Atlas (recomendado):
MONGODB_URI="mongodb+srv://dbAdmin:NjcyEKTrJRFRWfVG@cluster0.x7lixdk.mongodb.net/app"
# Para MongoDB local:
# MONGODB_URI=mongodb://localhost:27017/ag-sistemas

# Database Name (opcional se estiver na URI)
MONGODB_DB_NAME=ag-sistemas

# Admin Authentication Token
# Gere um token seguro: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_TOKEN=seu_token_secreto_super_seguro_aqui_minimo_32_caracteres

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# JWT Configuration (Obrigatório para autenticação)
# Gere um secret seguro: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=seu_jwt_secret_super_seguro_minimo_32_caracteres
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
